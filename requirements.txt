# Runtime dependencies. Mirrors [project.dependencies] in pyproject.toml —
# `pip install -e .` is equivalent and preferred.
#
# torch: install the build that matches your CUDA version FIRST, from
# https://pytorch.org/get-started/locally/ — the generic wheel below may be
# CPU-only, which makes feature extraction ~20x slower.
torch>=2.1
numpy>=1.24
pandas>=2.0
pillow>=10.0
flask>=3.0
opencv-python>=4.9
open_clip_torch>=2.24
mediapipe>=0.10
ultralytics>=8.2
tqdm>=4.65
rawpy>=0.18

# pyexiv2 is REQUIRED, not optional: RAW files carry DateTimeOriginal only in the
# container (their embedded preview doesn't have it), so without this every RAW
# frame silently becomes its own single-frame burst. It also supplies the Sony
# lens model that PIL won't surface.
pyexiv2>=2.8
